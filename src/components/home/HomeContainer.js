import React from 'react';

//FIXME: Look into SearchBarContainer. Here actions airingtv and airingtheater con't be called from onclick but when we launch the app 1st time (not on every call to home)
